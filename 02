scores <- data.frame(
  name = c("Anna", "Ben", "Chris"),
  math = c(90, 85, 88),
  english = c(85, 80, 82),
  science = c(92, 78, 84),
  history = c(88, 90, 85),
  art = c(91, 87, 89)
)

#Q. 다음 scores 데이터프레임은 학생별로 다섯 과목의 점수를 담고 있다. 각 학생의 최고 점수를 계산하는 코드를 작성하시오.
scores %>%
  rowwise() %>%
  mutate(max_score = max(across(math:art))) %>%
  select(name, max_score)
