df <- data.frame(
  math = c(70, 85, 60),
  english = c(90, 80, 75),
  science = c(88, 92, 70)
)

#Q. 각 학생이 세 과목 중에서 받은 점수 중 가장 높은 점수를 max_score라는 열로 추가하세요.
df %>%
  rowwise() %>%
  mutate(max_score = max(c_across(math:science)))
