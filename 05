library(ggplot2)
head(mpg)

#Q. mpg 데이터에서 구동 방식이 전륜구동(f)인 차량과 후륜구동(r)인 차량의 고속도로 연비(hwy)에 차이가 있는지 t-검정으로 분석하세요.
mpg_sub <- mpg %>%
  filter(drv %in% c("f", "r")) %>%
  select(drv, hwy)

t.test(hwy ~ drv, data = mpg_sub) # t-검정 수행
