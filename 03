# 예시 데이터프레임 생성
students <- data.frame(
  name = c("Alice", "Bob", "Carol", "David", "Eva"),
  math = c(90, 70, NA, 60, NA),
  english = c(85, NA, 75, 65, NA),
  korean = c(NA, 80, 85, 70, NA)
)

#Q. 결측값을 포함한 상태에서 평균 점수를 계산할 때, 결측값을 무시하고 계산하시오
students %>%
  mutate(na_avg = rowMeans(across(math:korean), na.rm=TRUE))

